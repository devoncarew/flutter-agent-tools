{
  "name": "flutter-agent-tools",
  "version": "0.1.0",
  "description": "Flutter/Dart agent tooling: dependency health, API inspection, and UI runtime introspection.",
  "mcpServers": {
    "flutter-agent": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/start_mcp_server.sh"
    }
  }
}
